// index.js

import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // This assumes App.js is in the same directory

ReactDOM.render(<App />, document.getElementById('root'));
