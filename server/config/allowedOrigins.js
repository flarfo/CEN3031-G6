// Define allowed origins here, these are the origins that can make API requests to the server
const allowedOrigins = [
    'http://localhost:3500', // local server for backend
    'http://localhost:3000' // local server for front-end
];

module.exports = allowedOrigins;